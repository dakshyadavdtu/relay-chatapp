# Legacy duplicate — DO NOT USE

This folder is a **legacy duplicate** of the chat UI. **Never import** from here.

The real chat lives in **`src/features/chat`** (ChatRoot and related UI).

Quarantined in Phase C3 to prevent accidental use.
